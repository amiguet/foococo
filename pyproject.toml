[project]
name = "foococo"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "pyo>=1.0.5",
]

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "foococo"
module-root = ""
